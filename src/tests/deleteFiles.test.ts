// import { deleteFiles } from '../helpers/deleteFile.ts';
import { sleep } from '@/src/helpers/sleep.ts';
// import { makeFakeDir } from '@/src/tests/makeFakeDir.ts';

// Deno.test('deleteFiles test', async () => {
// 	// const dir = `${Deno.cwd()}/dir_tests`
// 	const files = [];
// 	try {
// 		// throw new Error("error.message");
// 		const { userDir, filesDir, users } = await makeFakeDir();
// 		// const us1 = co 

// 		for await (const [idx] of (new Array(10)).entries()) {
// 			const nameFile = idx + 'test.txt';
// 			await Deno.writeTextFile(`${userDir}/${nameFile}`, `${nameFile}\n`);
// 			files.push(nameFile);
// 			// await sleep(200)
// 		}

// 		await sleep(1200);
// 		// await deleteFiles(dir, files, 'dir_tests');
// 	} catch (error) {
// 		throw new Error(error.message);
// 	}
// });
